{
  "private": true,
  "name": "@elm-i18n/translatable-html",
  "version": "0.0.0",
  "scripts": {
    "docs": "elm-doc-preview",
    "lint": "elm-format src --validate",
    "validate": "npm run lint && npm run validate:build && npm run validate:docs",
    "validate:build": "elm make",
    "validate:docs": "elm-doc-preview --output /dev/null"
  },
  "workspaces": [
    "benchmarks",
    "example"
  ],
  "devDependencies": {
    "elm": "0.19.1-5",
    "elm-doc-preview": "^5.0.5",
    "elm-format": "^0.8.5"
  }
}
